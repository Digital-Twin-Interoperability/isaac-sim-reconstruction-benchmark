apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: yizhan-isaacsim-workspace
  namespace: nsf-maica
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 50Gi
